<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ark.monitor.bug.repoter.statistics.mapper.StatisticsRealtimeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ark.monitor.bug.repoter.statistics.entity.StatisticsRealtime">
        <result column="id" property="id" />
        <result column="status" property="status" />
        <result column="msg_id" property="msgId" />
        <result column="error_count" property="errorCount" />
        <result column="project_name" property="projectName" />
        <result column="handler" property="handler" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,status,msg_id, error_count, project_name, handler,create_time,update_time
    </sql>

</mapper>
